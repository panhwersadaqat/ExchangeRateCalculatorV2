package com.example.data.repository.wifi

import javax.inject.Inject
import javax.inject.Singleton

/*
@Singleton
class WifiRepositoryImpl @Inject constructor(
    private val localSource: WifiLocalSource
): WifiRepository{
    override suspend fun saveWifiInfo(info: WifiInfoRequest) {
        localSource.saveWifiInfo(info)
    }

    override suspend fun getWifiInfo(): Flow<WifiInfoRequest?> {
        return localSource.getWifiInfo()
    }
}*/
