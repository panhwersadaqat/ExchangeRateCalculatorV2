package com.example.data

/*
import com.example.data.datastore.DataStoreManager
import com.repository.UserSession

import kotlinx.coroutines.flow.first
import javax.inject.Inject
import javax.inject.Singleton

@Singleton
class UserSessionImpl @Inject constructor(var dataStoreManager: DataStoreManager) : UserSession {
    override suspend fun isUserLoggedIn() = dataStoreManager.getToken().first() != null
    override suspend fun getToken(): String? = dataStoreManager.getToken().first()
    override suspend fun getFCMToken(): String? = dataStoreManager.getFCMToken().first()
    // override suspend fun getProfile():  Flow<ProfileResponse?> = dataStoreManager.getObject<ProfileResponse>(PreferenceKeys.PROFILE)
    // override suspend fun getActivityStatus() : Flow<Boolean?> = dataStoreManager.getChatActivityStatus()

}*/
